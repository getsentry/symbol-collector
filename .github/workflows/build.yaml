name: build
on:
  push:
    branches:
      - main
      - release/*

  pull_request:

env:
  SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}

jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2
      - uses: microsoft/setup-msbuild@6fb02220983dee41ce7ae257b6f4d8f9bf5ed4ce # v2
        if: matrix.os == 'windows-latest'

      # Needed for Android SDK setup step
      - uses: actions/setup-java@17f84c3641ba7b8f6deff6309fc4c864478f5d62 # v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@07976c6290703d34c16d382cb36445f98bb43b1f # v3.2.0
        with:
          log-accepted-android-sdk-licenses: false
          # The action defaults to "tools platform-tools", but the obsolete "tools"
          # package was removed from Google's repository and now fails the step.
          packages: platform-tools

      - name: Install .NET SDK
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4
        with:
          # .NET 11 is not preinstalled on the runners, so this installs the
          # prerelease SDK pinned in global.json (single source of truth).
          global-json-file: global.json

      - name: Install Workloads (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          sudo dotnet workload restore
          sudo chown -R $USER ~/.local/share/NuGet || true
          sudo chown -R $USER ~/.nuget || true

      - name: Install Workloads (Windows/Linux)
        if: matrix.os != 'macos-latest'
        run: dotnet workload restore

      # .NET 11's Android workload requires API 37, which the runners don't preinstall.
      - name: Install Android SDK platform (API 37)
        run: >
          dotnet build src/SymbolCollector.Android/SymbolCollector.Android.csproj
          -t:InstallAndroidDependencies
          -p:AcceptAndroidSDKLicenses=true

      - name: Run batch build script
        if: matrix.os == 'windows-latest'
        shell: cmd
        run: build.cmd

      - name: Run bash build script
        if: matrix.os != 'windows-latest'
        run: ./build.sh

      - name: Publish coverage report
        uses: codecov/codecov-action@ab904c41d6ece82784817410c45d8b8c02684457 # v3

      - name: Archive Artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        if: matrix.os == 'macos-latest'
        with:
          name: ${{ github.sha }}
          if-no-files-found: error
          path: |
            ${{ github.workspace }}/src/SymbolCollector.Console/*.zip
            ${{ github.workspace }}/src/SymbolCollector.Android/bin/Release/**/publish/*Signed.apk
            ${{ github.workspace }}/src/SymbolCollector.Android/obj/Release/**/*.so
            ${{ github.workspace }}/src/SymbolCollector.Server/publish/*.zip
